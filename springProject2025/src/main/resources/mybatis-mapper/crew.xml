<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

	<mapper namespace="com.example.springProject2025.mapper.BoardMapper">
		
		<!-- rally board.xml(대회 게시판) list-->
		<select id="selectCrewBoardList" parameterType="hashmap" resultType="com.example.springProject2025.model.Chat">
			SELECT *
		    FROM
		        CHAT  
		    <where>
		        ROOM_TYPE = 'GROUP'  
		        <if test="keyword != null and keyword.trim() != ''">
		            AND (
		                TITLE LIKE '%' || #{keyword} || '%'
		                OR NAME LIKE '%' || #{keyword} || '%'
		                OR INTRO LIKE '%' || #{keyword} || '%'
		                OR OWNER_ID LIKE '%' || #{keyword} || '%' 
		            )
		        </if>
		    </where>
		    ORDER BY CDATE DESC	    
		    OFFSET ( (#{page} - 1) * #{pageSize} ) ROWS 
		    FETCH NEXT #{pageSize} ROWS ONLY
		</select>
		
		<select id="selectCrewBoardCnt" parameterType="hashmap" resultType="int">
		    SELECT COUNT(*) AS CNT
		    FROM CHAT
		    WHERE 1=1
		  	AND ROOM_TYPE = 'GROUP'  
		    <if test="type != null and type != ''">
		        AND TYPE = #{type}
		    </if>
		    <if test="keyword != null and keyword != ''">
		        AND (
		            TITLE LIKE '%' || #{keyword} || '%'
		            OR INTRO LIKE '%' || #{keyword} || '%'
		        )
		    </if>
		</select>
			
		<!--crew update post -->
		<insert id="crewBoardPost" parameterType="hashmap">
			 INSERT INTO CHAT (
			    CHATROOM_NO,
			    ROOM_TYPE,
			    TITLE,
			    OWNER_ID,
			    CDATE,
			    NAME,
			    INTRO,
			    UDATE
			)
			VALUES (
			    CHAT_SEQ.NEXTVAL,
			    'GROUP',                 -- 항상 GROUP으로 고정
			    #{title, jdbcType=VARCHAR},
			    #{ownerId, jdbcType=VARCHAR},
			    SYSDATE,
			    #{name, jdbcType=VARCHAR},
			    #{intro, jdbcType=VARCHAR},
			    SYSDATE
			)
		</insert>

		
		<!-- crew delect post-->	
		<!--<delete id="deleteCrewPost" parameterType="hashmap">
			DELETE FROM CHAT
			WHERE CHATROOM_NO = #{chatroomNo}
		</delete>-->
			
	</mapper>  